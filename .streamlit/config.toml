# 앱 CSS가 밝은 배경(흰 카드 / #F5F6F8)을 전제로 작성돼 있어
# 보는 사람의 다크 모드에 따라 색이 뒤집히지 않도록 라이트 테마로 고정한다.
[theme]
base = "light"
primaryColor = "#1E3A5F"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F5F6F8"
textColor = "#16243A"
